# 变更记录

> 记录公开发布之后的实质变更。条目按时间倒序。

---

## 2026-09-22 — 首次公开发布 + 首轮独立审计修复

**首次发布**：20 个文件 / 约 2900 行，许可 CC BY 4.0。

发布当天做了**首轮独立审计**——12 个互不通气的审计员，各自取证、被要求找反例而非确认。以下是由此产生的修复：

### 隐私

- 移除正文里的**领域指纹**（未经改写的真实文件名、特定领域的符号组合）。它们不含敏感串，所以关键词门禁扫不到，但**足以指认来源项目**。
- 公开仓库历史里保留了 3 个被 amend 掉的旧 commit（GitHub 侧无法用 push 删除）——已核实其 tree 与 HEAD 完全相同，**未泄露任何额外内容**，仅含旧的本机用户名。

### 许可

- `LICENSE` 换为 **CC BY 4.0 官方全文**。此前是中文改写摘要，经实测 GitHub 识别为 `Other / NOASSERTION`——别人看不出能怎么用。
- README 的「来源」表述改为**与证据相称**（样本为 1，无对照实验）——原文写「验证过」，而那套配套框架不随本项目发布，读者无法核实。

### 正确性

- 统一「原则 N」的引用。公开版有**两套编号**（00 的 7 条精神内核、01 的 10 条协作原则），此前存在跨文档混用，导致「原则 6」指向了另一条。
- 原子项上限统一为**「12 推荐 / 15 绝对上限」**（此前同一节内两个数值打架）。
- 对话轮次阈值由 `~30` 修正为 **`~20`**——原值高于来源框架的全部三个角色阈值，会把警戒线推到已经退化之后。
- 修正 `trap ERR` 示例：**缺 `set -E` 就拿不到函数内的失败行号**（ERR trap 默认不继承进 shell 函数）。
- 修正「数处数」的命令：`grep -c` 数的是**行**，一行两处会少算；数处数要用 `grep -o … | wc -l`。

### 评估规范

- 报告骨架补 **p99 列**（此前缺，与自家铁律 3 直接冲突）。
- 反例对照表补 **「正确做法」列**（此前只说了坑，没说怎么爬出来）。
- 三条铁律补**统计前提**：尾部指标的样本量约束（n<100 时 p99 不可估）、阈值的多重比较校正、事后切换指标必须披露。

### 结构

- `06-质量与验收.md` 与 `reference/评估规范.md` 的逐字重复段落改为**单向引用**，消除「信息不唯一」。

### 新增

- README 的「**15 分钟上手**」——此前全仓库只有一个可直接复制粘贴的动作。
- **三份缺失的模板**：`qa-report` / `dev-status` / `completion-report`（文档说 qa-report「最不该省」，却没有它的模板）。
- `05-决策边界.md` 新增「**当你觉得它停机太频繁**」——规则被过度应用是这套方法最可能的弃用路径。
- 本文件。

---

## 尚未处理 / 已知限制

诚实列出**这一轮没做的事**，避免读者以为已经完备：

- **门禁只在私有侧**。本项目公开的只有方法论；那道发布门禁（`.publish/`）不随包发布，所以**本项目自己的规则仍然是纸面的**（见 README「使用前请读」）。
- **没有真实案例**。`07-实战切片.md` 的例子是虚构的——真实项目涉及保密，无法公开。这是本项目最大的证据短板。
- **角色模型针对「1 个人 + N 个 AI 对话」**。多真人协作的场景没有覆盖。
- **未做外部查重**。文档未与外部文本做逐字比对。
