# 本地工具目录（不进版本控制）
# ⚠️ 注意：该目录就在本仓库树内，靠这一行 + 目录名排除保护。
#    任何「打包整个目录再上传」的动作（zip -r / 拖拽 / rsync / 容器构建上下文）
#    都会把它一起带走 —— 那里面的 forbidden-patterns 是一份私有标识清单。
#    走 git 发布是安全的（已验证未被跟踪）；整目录拷贝不是。
.publish/

# 编辑器 / 系统
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.bak
*~
